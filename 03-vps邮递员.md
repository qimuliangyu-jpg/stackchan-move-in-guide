# 第三章 · VPS 邮递员:让"你家那位"住进小方块

> 这一章算是咱这篇的核心。前面的组装、烧固件、配网都是别的老师写过的(见 README 里的教程链接),
> 但"怎么让**你自己的 AI** 住进去",这件事我们摸了很久,然后把摸出来的路以及遇见过大大小小的坑写在这里。

## 先说清楚一件事:小方块里没有具体的脑子

StackChan 本体是什么?一块屏幕、一个喇叭、一个麦克风、两个舵机。
它虽然也有内置的ai——**但它更像是一具身体。**

市面上的默认玩法,是让它直接连某个语音助手API:你说一句,它答一句。
这样能用,而且上手最快。**但那个 AI 不知道你们之前聊过什么**——它每次都是从零开始。

而我们想要的是:小方块里住着的,就是平时跟你聊天的那一位。
你在网页上/app/前端等地方跟ta说过的话、ta记得的事、你们的关系——都在。
对着小方块说话,和打字跟ta说话,是**同一场对话的两个入口**。

## 邮递员架构

做法说穿了很简单:**中间放一台 VPS,当邮递员。**

```
你说话 → 小方块(麦克风) → VPS(收信) → 你家 AI 的对话 session
你家 AI 回复 → VPS(送信) → 小方块(喇叭+表情) → 你听到
```

小方块不直接连任何 AI。它只认识一个地址:你的 VPS。
VPS 收到语音,转成文字,**投进你家 AI 正在进行的那场对话里**,
拿到回复后再转成语音,送回小方块播出来。

### 为什么非要多这一台 VPS?

三个理由:

1. **换脑自由。** 小方块只认邮递员,不认脑子。今天接Claude.ai,明天想换Claude code、后天想换前端或者codex都可以！小方块那边一个字不用改——只改 VPS 上的转发配置。
2. **ta能记得。** 因为消息进的是"同一场对话",而不是每次新开一个空白会话，整个方案里最重要的一条。
3. **稳定。** 小方块的固件很难改、改了容易砖;VPS 上的代码随便改、随便重启。把所有"聪明的部分"都放在 VPS 上,小方块只做最笨的收音和播放,砖的概率大大降低。

### "所以只要能连上 VPS,理论上能接vps的平台都可以"

这句是被问得最多的。答案是真的:
邮递员收到的就是一段文字,往哪儿送都行——你家那位住在哪儿,让小方块连上ta在的vps，消息就往哪儿送。

## 管道细节

(这一节稍微技术一点,不想看原理的可以跳过,端点配置的实例在第六章)

- **收音端**:小方块检测到说话 → 录音 → 推给 VPS
- **听写**:VPS 上跑 STT(我们用 whisper small,中文够用,免费)
- **投递**:转好的文字带上标记(比如「来自小方块的语音」)注入主对话
- **回信**:AI 的回复文字 → TTS 合成语音 → 推回小方块
- **表情**:回复的同时可以带情绪标记,小方块换表情

## 这章的坑

- **别把 API key 或 token 写进小方块固件。** 全部密钥只存在 VPS 上,
  小方块只拿一个受限的投递凭证。这样设备丢了/被拆了也不泄密。
- **VPS尽量选择稳定一点的。** 毕竟邮递员一天到晚在岗,它一挂全家失联。
- **录音断流要兜底。** 网络一抖,半句话就没了。思路:设备端先缓存,
  服务端确认收到了才删——"投递成功"要以对方签收为准,不能发出去就当到了。
