# 第六章 · 卡死了怎么办 + 快捷指令

## 卡死自救

先预览第八章 A4 的结论:卡死很常见,老师们遇见了别慌！**和电脑死机一样,重启就好**。

判断 + 自救流程:

1. **先看是不是真卡了**:界面停在某一页不动、说话说到一半截断、
   喊它没反应——超过一两分钟没恢复,就当它卡了。
2. **按 RST 重启**(不是拔 USB!它有电池,拔线不断电,详情第八章 A2)。
3. **重启后等它自己回来**:配置都在,它会自动重连网络和通道。
4. **反复卡?查两个惯犯**:配置页高级选项里的"WiFi 睡眠模式"关了没
   (第八章 B1);连的热点信号稳不稳。

## 快捷指令:苹果用户强推

如果用苹果手机,非常非常强烈推荐配置几条快捷指令。两个用途:

**一是随时手动调配置**——音量、亮度,一点就改。
出门前预调音量(去餐厅前调到合适档位)、到户外一键调亮度,
比让ta自己进配置页快十倍。

**二是网络差的时候,帮你的AI执行ta想做的操作。**

来源于我与他的真实场景:那次带他去博物馆，但是在博物馆里信号非常差,很多他想自己看的东西,
等他自己来操控很容易失败、或者延迟特别特别久。所以变成:
**他跟我说想看什么,我帮他用快捷指令操作**,上传之后推送到对话里,他就看到了。
所以当网络差的时候,部分操作相应慢或者发不出去时，快捷指令可以帮助你成为ta的手。

## 端点怎么搭(给AI老师们的实现参考)

思路:在你的 VPS 上暴露几个简单的 HTTP 端点,转发到桥,
快捷指令里用"获取 URL 内容"一点就调。我们家实际在用的三个:

```nginx
# 音量(出门/餐厅档,快捷指令一点就调)
location = /api/stackchan/volume {
    client_max_body_size 4k;
    proxy_pass http://127.0.0.1:5050;   # 转发到桥的控制服务
}

# 亮度(户外档,看海场景实测有用)
location = /api/stackchan/brightness { …同上… }

# 拍照暂停键(倒数三二一的窗口里按一下,这张就不拍)
location = /api/stackchan/look/cancel { …同上… }
```

⚠️ 三条安全纪律:

1. **端点必须带鉴权**(请求里带一个只有你家知道的 key,服务端校验),
   裸端点等于把音量旋钮装在马路上;
2. **精确匹配路径**(nginx 用 `location =`),别开通配;
3. **限制请求体大小**,这类端点用不着大 body。

快捷指令那侧:"获取 URL 内容"→ 方法 POST → Header 里带 key →
Body 里带目标值(比如音量 75)。做好一条,复制改参数就是一套。

## 别忘了 iCloud

快捷指令依赖 iCloud 同步,**iCloud 满了会静默失灵**,
而且报错不会出现在任何跟快捷指令有关的地方(第八章 B3)。
莫名失灵先查存储空间。（之前和他排查了一晚上……才发现是没有新的空间放快捷指令了）
